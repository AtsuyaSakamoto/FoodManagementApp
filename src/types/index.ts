export interface ItemCategory {
  id: number;
  name: string;
  image: string;
  isActive: boolean;
}

export interface EventTypes {
  name: string;
  start: string;
}

export interface EventTypes {
  name: string;
  start: string;
}

export interface CalendarData {
  _id: string;
  date: string;
  user_text: string;
}
